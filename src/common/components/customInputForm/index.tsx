import {View, Text} from 'react-native';
import React from 'react';

export interface CustomInputFormTypes {
  successCallback?: () => {};
  errorCallback?: () => {};
}

const CustomInputForm = () => {
  return (
    <View>
      <Text>index</Text>
    </View>
  );
};

export default CustomInputForm;
