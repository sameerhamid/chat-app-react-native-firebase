import {StyleSheet} from 'react-native';
import {Colors} from '../../../common/model/theme/themeModel';

export type VidioCallScreenStyleTypes = {};

const styles = (colors?: Colors): VidioCallScreenStyleTypes =>
  StyleSheet.create<VidioCallScreenStyleTypes>({});

export default styles;
